# Voice AI Agent — Python dependencies
# Tested on Python 3.11 with CUDA 12.x / NVIDIA Riva 2.14+

# ---------------------------------------------------------------------------
# Web framework
# ---------------------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
httpx==0.27.0
python-multipart==0.0.9

# ---------------------------------------------------------------------------
# NVIDIA Riva ASR / TTS client
# ---------------------------------------------------------------------------
nvidia-riva-client==2.14.0

# ---------------------------------------------------------------------------
# LLM client (Ollama via HTTP)
# ---------------------------------------------------------------------------
# httpx already listed above — Ollama uses the REST API directly

# ---------------------------------------------------------------------------
# RAG: embeddings + FAISS
# ---------------------------------------------------------------------------
sentence-transformers==3.0.1
faiss-cpu==1.8.0
# For GPU FAISS, replace faiss-cpu with:
# faiss-gpu==1.7.4  (requires CUDA 11.x matching your driver)

numpy==1.26.4

# ---------------------------------------------------------------------------
# Async / utilities
# ---------------------------------------------------------------------------
aiofiles==23.2.1
structlog==24.1.0

# ---------------------------------------------------------------------------
# gRPC (pulled in by nvidia-riva-client; pinned for reproducibility)
# ---------------------------------------------------------------------------
grpcio==1.64.0
grpcio-tools==1.64.0
protobuf==4.25.3
