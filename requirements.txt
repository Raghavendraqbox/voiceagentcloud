# Voice AI Agent — Python dependencies
# Tested on Python 3.11, CUDA 12.4, PyTorch 2.6.0+cu124
#
# IMPORTANT — install order matters on GPU machines:
#   1. Install PyTorch with CUDA FIRST (see README step 4)
#   2. Then: pip install -r requirements.txt
# --------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Web framework
# ---------------------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
httpx==0.27.0
python-multipart==0.0.9

# ---------------------------------------------------------------------------
# NVIDIA Riva ASR / TTS  (cloud gRPC via NVCF — no local Docker needed)
# ---------------------------------------------------------------------------
nvidia-riva-client==2.14.0
grpcio==1.64.0
grpcio-tools==1.64.0
protobuf==4.25.3

# ---------------------------------------------------------------------------
# LLM clients
# ---------------------------------------------------------------------------
# Ollama — uses httpx (listed above), no extra package needed
# Anthropic Claude — fallback LLM
anthropic==0.88.0

# ---------------------------------------------------------------------------
# RAG: embeddings + FAISS
# ---------------------------------------------------------------------------
sentence-transformers==3.0.1
transformers>=4.40,<5.0     # pinned: kokoro breaks on transformers 5.x
faiss-cpu==1.8.0
# For GPU FAISS (optional, faster retrieval):
#   pip uninstall faiss-cpu && pip install faiss-gpu==1.7.4

numpy==1.26.4

# ---------------------------------------------------------------------------
# ASR fallback: Whisper (when Riva is unavailable)
# ---------------------------------------------------------------------------
faster-whisper==1.2.1

# ---------------------------------------------------------------------------
# TTS fallbacks: Kokoro GPU, then edge-tts
# ---------------------------------------------------------------------------
kokoro==0.9.4
misaki[en]>=0.9.2           # Kokoro G2P / phonemiser
edge-tts==7.2.8
av==17.0.0                  # PyAV: MP3 decode + PCM resample for edge-tts

# ---------------------------------------------------------------------------
# Async / utilities
# ---------------------------------------------------------------------------
aiofiles==23.2.1
structlog==24.1.0
